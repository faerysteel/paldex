{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Paldex needs only a folder picker; save reading happens in Rust.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
